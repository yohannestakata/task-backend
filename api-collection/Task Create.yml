info:
  name: Task Create
  type: http
  seq: 2

http:
  method: POST
  url: "{{URL}}/tasks"
  body:
    type: json
    data: |-
      {
        "title": "Clean my room",
        "description": "Need to clean each part of it"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
