info:
  name: Task Get
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}/tasks?status=OPEN"
  params:
    - name: status
      value: OPEN
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
